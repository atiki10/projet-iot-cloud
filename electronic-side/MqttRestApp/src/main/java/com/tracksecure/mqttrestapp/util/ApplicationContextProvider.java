package com.tracksecure.mqttrestapp.util;

/**
 * ApplicationContextProvider removed — websockets disabled.
 * Placeholder kept to avoid compile errors if referenced elsewhere.
 */
public final class ApplicationContextProvider {
    private ApplicationContextProvider() {}
}
